# MongoDB Configuration
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/ca_schools?retryWrites=true&w=majority

# Google Cloud Configuration
PROJECT_ID=your-google-cloud-project-id

# Application Configuration (optional)
PORT=8080

# Instructions:
# 1. Copy this file to .env
# 2. Replace the placeholder values with your actual credentials
# 3. Never commit the .env file to version control

# To get MongoDB URI:
# 1. Go to MongoDB Atlas (https://cloud.mongodb.com)
# 2. Create a cluster or use existing one
# 3. Click "Connect" -> "Connect your application"
# 4. Copy the connection string and replace <username> and <password>

# To get Google Cloud Project ID:
# 1. Go to Google Cloud Console (https://console.cloud.google.com)
# 2. Create a project or select existing one
# 3. Enable Vertex AI API
# 4. Set up Application Default Credentials or service account